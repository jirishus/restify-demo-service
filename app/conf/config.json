{
 "host": "$HOST",
 "port": "$PORT",
 "env": "$ENV",
 "apikey": "$APIKEY",
 "mongo": {
	"host": "$MONGOHOST",
	"collection": "$MONGOCOLLECTION"
 },
 "google": {
	 "maps_api_key": "$MAPS_API_KEY"
 }
}